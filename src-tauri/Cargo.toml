[package]
name = "galgame-dialogue-translator-tauri"
version = "0.1.0"
description = "Player-first galgame dialogue translator desktop shell"
authors = ["OpenAI Codex"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []
